import React from 'react';
import Spinner from 'react-native-loading-spinner-overlay';

const Loading = () => {
  return <Spinner visible={true} color="#28D3A7" overlayColor="#131313" />;
};

export default Loading;
